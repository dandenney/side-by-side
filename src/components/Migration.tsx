'use client'

export default function Migration() {
  // Migration system disabled - should be handled server-side
  // Client-side migrations are a security risk and not appropriate
  // for production applications
  return null
} 